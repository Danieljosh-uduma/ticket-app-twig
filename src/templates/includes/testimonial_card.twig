{% block content %}
    <div class="bg-white p-6 rounded-lg shadow-lg hover:shadow-[#e7008a21] text-gray-800">
        <p class="italic mb-4">"{{ feedback }}"</p>
        <p class="font-bold">{{ author }}</p>
        <p class="text-sm text-gray-600">{{ position }}</p>
    </div>
{% endblock %}